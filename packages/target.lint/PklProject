/// Typed modules for cross-language linters (markdownlint, commitlint, gitleaks).
amends "../basePklProject.pkl"

dependencies {
  ["target.core"] = import("../target.core/PklProject")
  ["pkl.toml"] { uri = "package://pkg.pkl-lang.org/pkl-pantry/pkl.toml@1.0.2" }
}

package {
  version = "0.1.0"
}
