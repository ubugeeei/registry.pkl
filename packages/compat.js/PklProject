/// Typed modules for JavaScript, TypeScript, and monorepo tooling.
amends "../basePklProject.pkl"

dependencies {
  ["compat.core"] = import("../compat.core/PklProject")
}

package {
  version = "0.1.0"
}
