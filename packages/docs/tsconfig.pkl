amends "@target.js/tsconfig/TsConfig.pkl"

compilerOptions {
  target = "ES2022"
  `module` = "ESNext"
  moduleResolution = "Bundler"
  strict = true
  noEmit = true
  types {
    "node"
  }
}

include {
  "vite.config.ts"
  "vitest.config.ts"
  "src/**/*.ts"
  "tests/**/*.ts"
}
