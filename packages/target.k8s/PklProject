/// Typed modules for Kubernetes manifests (Kustomize, Helm).
amends "../basePklProject.pkl"

dependencies {
  ["target.core"] = import("../target.core/PklProject")
}

package {
  version = "0.1.0"
}
