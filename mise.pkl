amends "@compat.env/mise/Mise.pkl"

tools {
  ["node"] = "24"
  ["pnpm"] = "10"
}

tasks {
  ["dev"] = new Task {
    description = "Start the documentation site"
    run = "pnpm dev"
  }
  ["build"] = new Task {
    description = "Build the documentation site"
    run = "pnpm build"
  }
  ["fmt"] = new Task {
    description = "Run formatting workflows"
    run = "pnpm fmt"
  }
  ["lint"] = new Task {
    description = "Run lint workflows"
    run = "pnpm lint"
  }
  ["check"] = new Task {
    description = "Run static checks"
    run = "pnpm check"
  }
  ["test"] = new Task {
    description = "Run tests"
    run = "pnpm test"
  }
  ["gen"] = new Task {
    description = "Compile Pkl manifests to ecosystem-native files"
    run = "pkl eval -m . root-files.pkl"
  }
  ["resolve"] = new Task {
    description = "Resolve local and remote Pkl package dependencies"
    run = "pkl project resolve . packages/compat.*"
  }
}
