# crates

Rust crates for `registry.pkl` should live here.

Expected early crates:

- registry validator
- search index builder
- metadata normalizer
- shared TOML emission helpers

Pkl integration is expected to go through `pklrust`.
